# Copy this file to .env and fill in your own keys.
# Never commit .env to version control.

# Required — OpenAI API key for GPT-4o Vision (exhibit recognition) and embeddings (RAG)
OPENAI_API_KEY=sk-...

# Optional — only needed if using ElevenLabs TTS backend (default is gTTS, which is free)
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=JBFqnCBsd6RMkjVDRZzb
